// ============================================
// app/(public)/layout.tsx
// Layout pour pages publiques (landing, auth) - sans sidebar
// ============================================

export default function PublicLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
